<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidhive.androidsqlite"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".Login"            
            >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ActivityMagg" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".Sincronizacion" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".NuevaFarmacia" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".MyAndroid" >
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" /><!--Permiso para que la aplicacion pueda acceder a internet -->
</manifest>